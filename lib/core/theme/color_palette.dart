import 'package:flutter/material.dart';

import 'app_colors.dart';

class ColorPalette {
  ColorPalette._();

  static const Color backgroundLight = AppColors.background;
  static const Color primaryButton = AppColors.button;
  static const Color dangerSos = AppColors.sos;
  static const Color safeBadge = AppColors.safe;
  static const Color cardBase = AppColors.card;
}
